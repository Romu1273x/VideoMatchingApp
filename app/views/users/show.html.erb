<div>
  <div>マイページ</div>
  <div>
    <h2><%= @user.hdl_name %></h2>
    <p>ユーザーID:<%= @user.user_id %></p>
    <p>自己紹介文</p>
    <p><%= @user.content %></p>
    <% if @user.id == @current_user.id %>
      <%= link_to("編集", "/users/#{@user.id}/edit") %>
    <% end %>
  </div>
</div>