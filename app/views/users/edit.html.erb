<div>
  <div>マイページ編集</div>
  <div>
    <%= form_tag("/users/#{@user.id}/update") do %>
        <p>ハンドルネーム</p>
        <input name="hdl_name" value="<%= @user.hdl_name %>">
        <p>自己紹介文</p>
        <textarea name="content"><%= @user.content %></textarea>
        <input type="submit" value="保存">
    <% end %>
  </div>
</div>